tensorflow model generator from YAML config file.

- python container
- generate model for classification
- will need to be binary (sigmoid) and multi-class (softmax)
    - metrics will be different (top K vs standard)